// vite.config.js

export default {
    // Especifica la ruta base para tu aplicación
    base: './',
  
    // Indica dónde se encuentran los archivos de origen
    root: 'resources',
  
    // Configura la ruta de salida de los archivos generados
    build: {
      outDir: '../public/dist',
    },
  };
  